{
  "name": "shopit",
  "version": "1.0.0",
  "description": "ecommerce site using mern stack",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "SET NODE_ENV=DEVELOPMENT& SET PORT=4000& nodemon backend/server ",
    "prod": "SET NODE_ENV=PRODUCTION& SET PORT=4000& nodemon backend/server ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ajay pradhan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.8"
  },
  "devDependencies": {
    "nodemon": "^2.0.13"
  }
}
